<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @domains.each do |domain| %>
    <tr>
        <td><%= domain.title %></td>
        <td><%= link_to 'Show', project_domain_path(domain.project_id, domain) %></td>
        <td><%= link_to 'Edit', edit_project_domain_path(@project, domain) %></td>
        <td><%= link_to 'Destroy', project_domain_path(:projects, domain), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
