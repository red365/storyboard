<p id="notice"><%= notice %></p>

<h1>Domains</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @domains.each do |domain| %>
    <tr>
        <td><%= domain.title %></td>
        <td><%= link_to 'Show', project_domain_path(domain.project_id, domain) %></td>
        <td><%= link_to 'Edit', edit_project_domain_path(domain.project_id, domain) %></td>
        <td><%= link_to 'Destroy', project_domain_path(domain.project_id, domain), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Domain', new_project_domain_path %>
